const fs = require('fs'); const css = fs.readFileSync('d:/ABSYS/test.css', 'utf8'); let open = 0; let lines = css.split('\n'); let unclosedLine = -1; for (let i = 0; i < lines.length; i++) { const line = lines[i]; for (let char of line) { if (char === '{') { if (open === 0) unclosedLine = i + 1; open++; } else if (char === '}') { open--; if (open === 0) unclosedLine = -1; } } } console.log('Unclosed block starts at line in test.css:', unclosedLine);
